/* =====================================================================
   Move Free Logistics — Supabase configuration
   ---------------------------------------------------------------------
   Replace the two placeholder values below with your own project's
   values from:  Supabase Dashboard → Project Settings → API
     • SUPA_URL  = "Project URL"        (e.g. https://abcdxyz.supabase.co)
     • SUPA_ANON = "anon / public key"  (a long token starting with eyJ...)
   These are PUBLIC values — safe to commit. Security is enforced by the
   database rules (RLS) you set up in SUPABASE_SETUP.md, not by hiding them.
   Until you fill these in, the tracking page falls back to tracking.json
   and the dashboard will show a "not configured" notice.
   ===================================================================== */
window.SUPA_URL  = "YOUR_SUPABASE_URL";
window.SUPA_ANON = "YOUR_SUPABASE_ANON_KEY";
